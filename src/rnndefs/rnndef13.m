function [layers, options, winSize, rnnDesc] = rnndef13
	layers = [
		sequenceInputLayer(8, 'Normalization', 'rescale-zero-one')
		lstmLayer(50, 'OutputMode', 'last')
		fullyConnectedLayer(1)
		regressionLayer
	];
	options = trainingOptions('adam', ...
		'MiniBatchSize', 300, ...
		'InitialLearnRate', 0.1, ...
		'Shuffle', 'every-epoch', ...
		'SequencePaddingDirection', 'left' ...
	);
	winSize = 10;
	rnnDesc = "Normalization: rescale-zero-one";
end
